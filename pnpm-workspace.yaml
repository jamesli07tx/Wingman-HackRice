# MERGE CONTRACT (DESIGN_WINDOWS.md §0): glassbridge/ is deliberately NOT a
# workspace package — it is Swift, owned by the Mac build. Never add it here.
packages:
  - shared
  - cortex
  - console
  - ui
  - corpus
allowBuilds:
  esbuild: true
  sharp: true
