# Wingman shared .env — DESIGN.md Appendix A
# Rename/copy this file to `.env` at the repo root. LOCAL ONLY — never commit `.env`.
# The checked-in template is .env.example (Windows agent owns it). Each package reads only what it needs.

# Anthropic (cortex, corpus scripts) — console.anthropic.com; needs claude-opus-5 + claude-haiku-4-5 access
ANTHROPIC_API_KEY=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

# Tavily live search (cortex, corpus scripts) — app.tavily.com
TAVILY_API_KEY=keen_7sc6bjbp5g_5c0fc7d8f1cd22471326502c79dcf3c6f0ec9da11acf5c3614107b92457a0e14

# Supabase (cortex, corpus scripts) — project settings → API
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=    # server-side only, never in console

# Clerk — dashboard.clerk.com → API keys
CLERK_SECRET_KEY=             # cortex (JWT verify via @clerk/backend), console (server)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=   # console

# Cortex URLs (console → REST/WS) — filled after the Fly.io deploy lands
NEXT_PUBLIC_CORTEX_URL=       # e.g. https://wingman-cortex.fly.dev
NEXT_PUBLIC_CORTEX_WS_URL=    # e.g. wss://wingman-cortex.fly.dev

PORT=8080                     # cortex

# glassbridge: CORTEX_URL / CORTEX_WS_URL live in glassbridge/Config.local.xcconfig on the Mac, not here
